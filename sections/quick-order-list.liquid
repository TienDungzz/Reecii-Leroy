{{ 'quick-order-list.css' | asset_url | stylesheet_tag: preload: true }}
{{ 'quantity-popover.css' | asset_url | stylesheet_tag: preload: true }}

{% render 'quick-order-list',
  product: product,
  show_image: section.settings.show_image,
  show_sku: section.settings.show_sku
%}

{% schema %}
{
  "name": "t:names.quick-order-list",
  "limit": 1,
  "enabled_on": {
    "templates": ["product"]
  },
  "settings": [
    {
      "type": "checkbox",
      "id": "show_image",
      "default": false,
      "label": "t:settings.show_images"
    },
    {
      "type": "checkbox",
      "id": "show_sku",
      "default": false,
      "label": "t:settings.show_skus"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:content.section_padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "t:settings.top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "t:settings.bottom",
      "default": 36
    }
  ],
  "presets": [
    {
      "name": "t:names.quick-order-list"
    }
  ]
}
{% endschema %}
