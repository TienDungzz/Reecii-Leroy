<link rel="stylesheet" href="{{ 'component-cart-items.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-search.css' | asset_url }}" media="print" onload="this.media='all'">

{{ 'section-header.css' | asset_url | stylesheet_tag: preload: true }}

{%- liquid
  assign sec_stts = section.settings

  assign order = 'multi_tab_menu,logo,mobile_logo,menu,message,account,search,localization,mobile_search,actions'

  if shop.customer_accounts_enabled
    assign order = 'multi_tab_menu,mobile_search,logo,mobile_logo,menu,message,account,search,localization,actions'
  endif

  assign rows = 'top,bottom' | split: ','
  assign search_style = 'none'

  if section.settings.show_search
    assign search_style = 'modal'
  endif

  if sec_stts.enable_transparent_header_home and template.name == 'index'
    assign transparent = 'always'
    assign transparent_color_scheme = sec_stts.home_color_scheme
  elsif sec_stts.enable_transparent_header_product and template.name == 'product'
    assign transparent = 'always'
    assign transparent_color_scheme = sec_stts.product_color_scheme
  elsif sec_stts.enable_transparent_header_collection and template.name == 'collection'
    assign transparent = 'always'
    assign transparent_color_scheme = sec_stts.collection_color_scheme
  endif

  if sec_stts.enable_sticky_header == 'always'
    assign sticky = 'always'
  elsif sec_stts.enable_sticky_header == 'scroll-up'
    assign sticky = 'scroll-up'
  endif

  if transparent and sticky
    assign transparent = 'not-sticky'
  endif

  if sticky
    assign header_wrapper = 'sticky-header'
  else
    assign header_wrapper = 'div'
  endif

  if transparent
    if transparent_color_scheme == 'inverse'
      assign transparent_color_scheme = 'color-' | append: section.settings.color_scheme_transparent
    else
      assign transparent_color_scheme = 'color-' | append: section.settings.color_scheme_top
    endif
  endif

  capture logo
    render 'header-logo', header_settings: sec_stts, blocks: section.blocks
  endcapture

  capture menu
    render 'header-menu-snippet', header_settings: sec_stts, blocks: section.blocks
  endcapture

  capture mobile_menu
    render 'header-menu-snippet', header_settings: sec_stts, blocks: section.blocks, variant: 'mobile'
  endcapture

  capture account
    render 'header-account', settings: sec_stts, enable_account: sec_stts.show_account
  endcapture

  capture actions
    render 'header-functions', settings: sec_stts, blocks: section.blocks, enable_cart: true, enable_wishlist: settings.show_wishlist
  endcapture

  if shop.customer_accounts_enabled
    assign search_class = 'max-md:hidden'
  endif

  capture search
    render 'header-search', style: search_style, input_id: 'Search-In-Modal', class: search_class
  endcapture

  capture multi_tab_menu
    render 'header-multi-site', settings: sec_stts, blocks: section.blocks
  endcapture

  capture message
    render 'header-message', settings: sec_stts, blocks: section.blocks
  endcapture

  # Handle localization positioning
  if sec_stts.localization_position == 'left'
    if sec_stts.search_position == 'left'
      if shop.customer_accounts_enabled
        assign order = 'multi_tab_menu,mobile_search,logo,mobile_logo,message,account,search,localization,menu,actions'
      else
        assign order = 'multi_tab_menu,logo,mobile_logo,message,account,search,localization,menu,mobile_search,actions'
      endif
    else
      if shop.customer_accounts_enabled
        assign order = 'multi_tab_menu,mobile_search,logo,mobile_logo,menu,message,account,search,localization,actions'
      else
        assign order = 'multi_tab_menu,logo,mobile_logo,menu,message,account,search,localization,mobile_search,actions'
      endif
    endif
  else
    if sec_stts.search_position == 'left'
      if shop.customer_accounts_enabled
        assign order = 'multi_tab_menu,mobile_search,logo,mobile_logo,message,account,search,menu,localization,actions'
      else
        assign order = 'multi_tab_menu,logo,mobile_logo,message,account,search,menu,localization,mobile_search,actions'
      endif
    endif
  endif

  # Handle logo row
  if sec_stts.logo_row == 'bottom'
    capture mobile_logo
      render 'header-logo', header_settings: sec_stts, blocks: section.blocks, class: 'thinh-logo lg:none'
    endcapture
  endif

  # Skip mobile search under specific conditions:
  # Always render if search_row is bottom
  # Don't render when shop.customer_accounts_enabled is false and search_position is left
  assign skip_mobile_search = false
  if shop.customer_accounts_enabled == false and sec_stts.search_position == 'left' and search_style != 'none'
    assign skip_mobile_search = true
  endif

  if sec_stts.search_row == 'bottom' or search_style != 'none' and skip_mobile_search == false
    # Mobile search duplicate for when search is not in the right order for mobile layout
    capture mobile_search
      render 'header-search', class: 'lg:none', input_id: 'Search-In-Modal-Mobile', style: search_style, variant: 'mobile'
    endcapture
  endif

  assign show_language = sec_stts.show_language
  if localization.available_languages.size <= 1
    assign show_language = false
  endif

  assign show_country = sec_stts.show_country
  if localization.available_countries.size <= 1
    assign show_country = false
  endif

  if show_country or show_language
    assign show_flag = false
    if sec_stts.country_selector_style == true
      assign show_flag = true
    endif
    capture localization_markup
      render 'dropdown-localization', show_country: show_country, show_language: show_language, country_style: show_flag, localization_position: sec_stts.localization_position, localization_type: sec_stts.localization_type, hide_mobile: true
    endcapture
  endif

  assign bottom_row_blocks = ''

  if section.settings.menu_row == 'bottom'
    assign bottom_row_blocks = bottom_row_blocks | append: 'menu,'
  endif

  if search_style != 'none'
    if section.settings.search_row == 'bottom'
      assign bottom_row_blocks = bottom_row_blocks | append: 'search,'
    endif
  endif

  if section.settings.show_country or section.settings.show_language
    if section.settings.localization_row == 'bottom'
      assign bottom_row_blocks = bottom_row_blocks | append: 'localization,'
    endif
  endif

  assign bottom_row_blocks = bottom_row_blocks | split: ',' | compact

  if section.settings.section_height == 'compact'
    assign header_height_class = ' header--compact'
  endif

  if bottom_row_blocks.size > 0 and section.settings.color_scheme_top.settings.background.rgb == section.settings.color_scheme_bottom.settings.background.rgb and section.settings.divider_width == 0
    assign collapse_header_paddings_class = 'header--collapse-row-paddings'
  endif

  assign class = 'header'

  if transparent_color_scheme
    assign class = class | append: ' ' | append: transparent_color_scheme
  endif

  if sec_stts.color_scheme_top.settings.background.alpha != 1
    assign class = class | append: ' ' | append: 'header--inherit-color-scheme-on-menu-open'
  endif

  if header_height_class
    assign class = class | append: ' ' | append: header_height_class
  endif

  if collapse_header_paddings_class
    assign class = class | append: ' ' | append: collapse_header_paddings_class
  endif
-%}

<{{ header_wrapper }}
  class="
    header-wrapper
    {% if sec_stts.show_line_separator %} header-wrapper--border-bottom{% endif %}
  "
  {% if sticky %}
    data-sticky-type="{{ sticky }}"
  {% endif %}
>
  <header
    class="{{ class }}"
    {% if transparent %}
      transparent="{{ transparent }}"
    {% endif %}
  >
    <div class="row-group" style="{% render 'spacing-padding', settings: sec_stts %}">
      {% for row in rows %}
        {% liquid
          assign scheme = 'color_scheme_' | append: row
          assign class = 'header__row header__row--[row] color-[row_scheme] section section--full-width-margin section--[section-width]' | replace: '[row]', row | replace: '[row_scheme]', sec_stts[scheme] | replace: '[section-width]', sec_stts.section_width
          case row
            when 'top'
              assign first = mobile_menu

              if bottom_row_blocks.size > 0 and section.settings.divider_size == 'page-width'
                assign class = class | append: ' divider--page-width'
              endif

              if bottom_row_blocks.size > 0
                assign border_bottom_setting_id = 'divider_width'
              else
                assign border_bottom_setting_id = 'border_width'
              endif
            when 'bottom'
              assign first = ''
              assign class = class | append: ' max-md:hidden'

              assign border_bottom_setting_id = 'border_width'
          endcase

          assign style = '--border-bottom-width: [width]px; ' | replace: '[width]', section.settings[border_bottom_setting_id]

          if row == 'top'
            assign style = style | append: '--border-bottom-width-mobile: [width]px; ' | replace: '[width]', section.settings.border_width
          endif
        %}

        {% capture content %}
          {% render 'header-row',
            row: row,
            order: order,
            settings: sec_stts,
            first: first,
            logo: logo,
            mobile_logo: mobile_logo,
            menu: menu,
            actions: actions,
            localization: localization_markup,
            account: account,
            search: search,
            mobile_search: mobile_search,
            multi_tab_menu: multi_tab_menu,
            message: message
          %}
        {% endcapture %}

        {% assign content = content | strip %}
        {% if content != blank %}
          <div
            class="{{ class | strip }}"
            {%- if style != blank -%}
              style="{{ style | strip }}"
            {%- endif -%}
          >
            <div class="header__columns spacing-style">
              {{ content }}
            </div>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </header>
</{{ header_wrapper }}>

{%- if settings.cart_type == 'notification' -%}
  {%- render 'cart-notification', color_scheme: sec_stts.color_scheme -%}
{%- endif -%}

{%- stylesheet -%}
  .header[transparent] {
    --language-button-background-color: transparent;
    --language-button-border-color: transparent;

    /* used to apply transparency to .header__row, will only ever be transparent or unset */
    --header-bg-color: transparent;

    /* used to display the appropriate logo based on transparency state */
    --header-logo-display: none;
    --header-logo-inverse-display: block;

    position: absolute;
    top: 0;
    left: 0;
    right: 0;

    /* transparent color scheme on .header should never apply its background color */
    background-color: transparent;
    z-index: var(--layer-overlay);

    &[transparent='not-sticky'][data-sticky-state='active'],
    /* Only show opaque background when hovering over menu items with children (mega menu)
       Multiple selectors for performance: each simple :has() check is faster than one complex selector with multiple conditions */

    &:hover,
    &:has(.header__icon--menu button.active),
    &:has(.mega-menu__list:hover),
    &:has(.list-menu__item[aria-haspopup]:is(:hover, [aria-expanded='true'], [data-animating])),
    &:has(.menu-list__list-item[slot='overflow'] .list-menu__item:is(:hover, [aria-expanded='true'], [data-animating])) {
      --header-logo-display: unset;
      --header-logo-inverse-display: unset;
      --header-bg-color: unset;
      --color-foreground: inherit;
      --color-foreground-rgb: inherit;
      --color-background: inherit;
      --color-background-rgb: inherit;
      --color-border: inherit;
      --color-border-rgb: inherit;
      --color-primary-button-text: var(--color-foreground);
      --color-primary-button-hover-text: var(--color-foreground);

      .header__row:after {
        opacity: var(--opacity-100);
        /* Faster transition for "in" animation */
        transition: opacity var(--animation-values-fast), color var(--animation-values-fast),
          border-color var(--animation-values-fast), background-color var(--animation-values-fast);
      }
    }

    .header__row {
      background-color: transparent;
    }

    .header__row:after {
      /* Slower transition for "out" animation */
      transition: opacity var(--animation-values-slow), color var(--animation-values-slow),
        border-color var(--animation-values-slow), background-color var(--animation-values-slow);
    }
  }

  .header:has(.header__icon--menu button.active) {
    .header__icon:not(.header__icon--menu) {
      display: none;
    }
  }

  :is(.header[transparent]:not([data-sticky-state='active']), .header[transparent='always'][data-sticky-state='active'])
    .header__row:not(
      :has(
          .mega-menu__list:hover,
          .list-menu__item[aria-haspopup]:is(:hover, [aria-expanded='true'], [data-animating]),
          .menu-list__list-item[slot='overflow'] .list-menu__item:is(:hover, [aria-expanded='true'], [data-animating])
        )
    ) {
    /* while transparent, header-row ignores its color-scheme settings, pull from the header-component */
    --color-foreground: inherit;
    --color-foreground-rgb: inherit;
    --color-border: inherit;
    --color-border-rgb: inherit;
    --color-primary-button-background: inherit;
    --color-primary-button-text: inherit;
    --color-primary-button-border: inherit;
    --color-primary-button-hover-background: inherit;
    --color-primary-button-hover-border: inherit;
    --color-primary-button-hover-text: inherit;
  }

  /* section-header */
  .section-header-main.shopify-section-group-header-group {
    z-index: 3;
  }

  .shopify-section-header-sticky {
    position: sticky;
    top: 0;
  }

  .shopify-section-header-hidden {
    transform: translateY(calc(-1 * var(--header-height)));

    &.menu-open {
      transform: translateY(0);
    }
  }

  .section-header-main.animate {
    transition: transform var(--tst-smooth);
  }

  .shopify-section-group-header-group {
    z-index: 4;
  }

  .section-header-main ~ .shopify-section-group-header-group {
    z-index: initial;
  }

  .section-header-main.scrolled-past-header {
    .header__row {
      transition-delay: 0.4s;
    }
  }

  .section-header-main:not(.scrolled-past-header) {
    .header__row {
      transition-delay: 0s;
    }
  }

  .header__row {
    position: relative;
    background-color: var(--header-bg-color, var(--color-background));

    /* will default to bg from its color scheme unless --header-bg-color is transparent */
    &:after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: var(--header-bg-color, var(--color-background));
      z-index: -1;
      opacity: var(--opacity-0);
      pointer-events: none;
    }
  }

  /* Header padding styles */
  .header {
    --last-item-minus-margin: 1.2rem;
    .header__row--top {
      padding-block-start: var(--padding-block-start);
    }
    .header__row--bottom {
      padding-block-end: var(--padding-block-end);
    }

    &:not(:has(.header__row--bottom)) .header__row--top {
      padding-block-end: var(--padding-block-end);
    }

    &:not(:has(.header__row--top)) .header__row--bottom {
      padding-block-start: var(--padding-block-start);
    }

    &:has(.header__row--top) .header__row--bottom {
      padding-block-start: var(--padding-block-end);
    }

    &:has(.header__row--bottom) .header__row--top {
      padding-block-end: var(--padding-block-start);
    }
  }

  /* Animated Sticky Header */
  .section-header-main.scrolled-past-header sticky-header[data-sticky-type='always'] {
    .header {
    }
  }

  .header__row--top:not(.divider--page-width),
  .header__row--top.divider--page-width .header__columns,
  .header__row--bottom {
    border-bottom: var(--border-bottom-width) solid var(--color-border);
  }

  .header__row--bottom .header__column--right > *:last-child {
    margin-right: calc(var(--last-item-minus-margin) * -1);
  }

  @media screen and (max-width: 749px) {
    .header__row--top:not(.divider--page-width),
    .header__row--top.divider--page-width .header__columns {
      border-bottom-width: var(--border-bottom-width-mobile);
    }
  }

  .header__row.divider--page-width:not(.section--page-width) .header__columns:not(:has(.header-multi-site)) {
    @media screen and (min-width: 750px) {
      padding-inline-start: 0;
      padding-inline-end: 0;
      margin-inline-start: var(--page-margin);
      margin-inline-end: var(--page-margin);
    }
  }

  .header__row.divider--page-width:not(.section--page-width) .header__columns:has(.header-multi-site) {
    padding-inline: 0;

    @media screen and (min-width: 1025px) {
      padding-inline-start: 0;
      padding-inline-end: var(--page-margin);
    }
  }

  .header__column {
    display: flex;
    align-items: center;

    /* on mobile, header__column nodes are ignored to create a new grid-template-area based on all visible content */
    @media screen and (max-width: 1024px) {
      display: contents;
    }
  }

  .header__column--left,
  .header__column--center {
    gap: var(--gap-xl);
    grid-area: left;
  }

  .header__column--center {
    justify-content: center;
    grid-area: center;

    header-menu:only-child .overflow-menu::part(list) {
      justify-content: center;
    }
  }

  .header__column--right {
    gap: var(--gap-xl);
    justify-content: flex-end;
    grid-area: right;

    .overflow-menu::part(list) {
      justify-content: flex-end;
    }
  }

  .header__columns {
    /* Three column layout */
    --header-left: 1fr;
    --header-center: auto;
    --header-right: 1fr;
    --header-template-columns: var(--header-left) var(--header-center) var(--header-right);

    /* Mobile layout */
    --header-mobile-bookend: 44px;

    display: grid;
    grid-template-areas: 'left center right';
    grid-gap: var(--gap-xl);
    grid-template-columns: var(--header-template-columns);

    /* If menu is in center column */
    &:has(.header__column--center .header-menu) {
      --header-center: auto;
      --header-left: minmax(max-content, 1fr);
      --header-right: minmax(max-content, 1fr);
    }

    /* If search and no menu in center column */
    &:has(.header__column--center .header__search):not(:has(.header__column--center .header-menu)) {
      --header-center: minmax(max-content, 1fr);
      --header-left: minmax(max-content, 1fr);
      --header-right: minmax(max-content, 1fr);

      .header__search,
      .search-action {
        width: 100%;
        justify-content: center;
      }
    }

    &:has(.header__column--center .header__search) {
      .header__search .svg-wrapper {
        margin-inline-start: 0;
      }
    }

    &:has(.header__column--left .header__search .search--inline) {
      .header__search .details__list {
        left: 0;
      }

      .header__search .search-modal__form:has(.search__input.field__input:focus) .details__list,
      .header__search .search-modal__form[open] .details__list {
        opacity: 1;
        transform: translate3d(0, 1rem, 0);
        pointer-events: auto;
      }
    }

    &:has(.header__column--center .header__search .search--inline) {
      .header__search .predictive-search--center {
        width: 100%;
      }

      .header__search .details__list {
        left: 50%;
        transform: translate(-50%, 5rem);
      }

      .header__search .search-modal__form:has(.search__input.field__input:focus) .details__list,
      .header__search .search-modal__form[open] .details__list {
        transform: translate3d(-50%, 1rem, 0);
      }
    }

    &:has(.header__column--right .header__search .search--inline) {
      .header__search .details__list {
        right: 0;
      }

      .header__search .search-modal__form:has(.search__input.field__input:focus) .details__list,
      .header__search .search-modal__form[open] .details__list {
        transform: translate3d(0, 1rem, 0);
      }
    }

    &:has(.header__search .search--inline) {
      .search__input.field__input {
        padding: 1.5rem 0 1.5rem 2rem;
      }

      .header__search .details__list {
        opacity: 0;
        transition: opacity var(--animation-speed) var(--animation-easing),
          transform var(--animation-speed) var(--animation-easing);
          pointer-events: none;
      }

      .header__search .search-modal__form:has(.search__input.field__input:focus) .details__list,
      .header__search .search-modal__form[open] .details__list {
        opacity: 1;
        pointer-events: auto;
      }

      .predictive-search {
        padding-inline: var(--padding-2xl);
      }
    }

    /* If menu and search in center column */
    &:has(.header__column--center .header-menu):has(.header__column--center .header__search) {
      --header-center: minmax(max-content, 2fr);
      --header-left: minmax(max-content, 1fr);
      --header-right: minmax(max-content, 1fr);

      .header__search {
        width: 30rem;
      }

      .header__column--center {
        justify-content: space-between;
      }

      .predictive-search-results__list-inline {
        --card-grid-per-row: 2;
      }
    }

    /* If there is no center column, make the column the menu is in grow eagerly */
    &:where(:not(:has(.header__column--center))) {
      @media screen and (min-width: 750px) {
        --header-template-columns: var(--header-left) var(--header-right);

        grid-template-areas: 'left right';
      }

      /* If the header-menu is in the right column */
      &:has(.header__column--right header-menu) {
        --header-right: auto;
        --header-left: minmax(max-content, 1fr);
      }

      /* If the header-menu is in the left column */
      &:has(.header__column--left header-menu) {
        --header-left: auto;
        --header-right: minmax(max-content, 1fr);
      }
    }

    @media screen and (max-width: 1024px) {
      --header-template-columns: var(--header-mobile-bookend) var(--header-mobile-bookend) 1fr
        var(--header-mobile-bookend) var(--header-mobile-bookend);

      grid-template-areas: 'leftA leftB center rightA rightB';
      grid-column: span 3;
      grid-gap: 0;
      align-items: center;
      padding-block: 0;
      padding-inline: 0 var(--padding-3xs);

      .header-logo {
        grid-area: center;
        text-align: center;
      }

      &:not(:has(.header-actions)) .search-action {
        grid-area: leftB;
      }

      .account-actions,
      &:not(:has(.account-actions)) .search-action {
        grid-area: rightA;
      }

      .search-action {
        grid-area: leftB;
      }

      .header-actions {
        grid-area: rightB;
      }

      &:has(.header-multi-site) {
        .header-multi-site {
          grid-column: 1 / 6;
          grid-row: 1;
        }

        .header-logo,
        &:not(:has(.header-actions)) .search-action,
        .account-actions,
        &:not(:has(.account-actions)) .search-action,
        .search-action,
        .header-actions {
          grid-row: 2;
        }
      }
    }
  }

  /* Single column layout if there are no columns within */
  .header__columns:not(:has(.header__column)) {
    grid-template-columns: 1fr;
  }
{%- endstylesheet -%}

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if settings.logo %}
      "logo": {{ settings.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_twitter_link | json }},
      {{ settings.social_facebook_link | json }},
      {{ settings.social_pinterest_link | json }},
      {{ settings.social_instagram_link | json }},
      {{ settings.social_tiktok_link | json }},
      {{ settings.social_tumblr_link | json }},
      {{ settings.social_snapchat_link | json }},
      {{ settings.social_youtube_link | json }},
      {{ settings.social_vimeo_link | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>

{%- if request.page_type == 'index' -%}
  {% assign potential_action_target = request.origin | append: routes.search_url | append: '?q={search_term_string}' %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}

{% javascript %}
  class StickyHeader extends HTMLElement {
    constructor() {
      super();
    }

    connectedCallback() {
      this.header = document.querySelector('.section-header-main');
      this.headerElement = this.header.querySelector('.header');
      this.headerIsAlwaysSticky = this.getAttribute('data-sticky-type') === 'always';
      this.headerBounds = {};

      this.setHeaderHeight();

      window.matchMedia('(max-width: 1024px)').addEventListener('change', this.setHeaderHeight.bind(this));

      if (this.headerIsAlwaysSticky) {
        this.header.classList.add('shopify-section-header-sticky');
      }

      this.currentScrollTop = 0;
      this.preventReveal = false;
      this.predictiveSearch = this.querySelector('predictive-search');

      this.onScrollHandler = this.onScroll.bind(this);
      this.hideHeaderOnScrollUp = () => (this.preventReveal = true);

      this.addEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.addEventListener('scroll', this.onScrollHandler, false);

      this.createObserver();
    }

    setHeaderHeight() {
      document.documentElement.style.setProperty('--header-height', `${this.headerElement.offsetHeight}px`);
    }

    disconnectedCallback() {
      this.removeEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.removeEventListener('scroll', this.onScrollHandler);
    }

    createObserver() {
      let observer = new IntersectionObserver((entries, observer) => {
        this.headerBounds = entries[0].intersectionRect;
        observer.disconnect();
      });

      observer.observe(this.header);
    }

    onScroll() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      if (this.predictiveSearch && this.predictiveSearch.isOpen) return;

      if (scrollTop > this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        this.headerElement.dataset.stickyState = 'active';
        if (this.preventHide) return;
        requestAnimationFrame(this.hide.bind(this));
      } else if (scrollTop < this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        this.headerElement.dataset.stickyState = 'active';
        if (!this.preventReveal) {
          requestAnimationFrame(this.reveal.bind(this));
        } else {
          window.clearTimeout(this.isScrolling);

          this.isScrolling = setTimeout(() => {
            this.preventReveal = false;
          }, 66);

          requestAnimationFrame(this.hide.bind(this));
        }
      } else if (scrollTop <= this.headerBounds.top) {
        this.header.classList.remove('scrolled-past-header');
        requestAnimationFrame(this.reset.bind(this));
        this.headerElement.dataset.stickyState = 'idle';
      }

      this.currentScrollTop = scrollTop;
    }

    hide() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-hidden', 'shopify-section-header-sticky');
      this.headerElement.dataset.stickyState = 'inactive';
    }

    reveal() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-sticky', 'animate');
      this.header.classList.remove('shopify-section-header-hidden');
    }

    reset() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.remove('shopify-section-header-hidden', 'shopify-section-header-sticky', 'animate');
    }
  }
  if (!customElements.get('sticky-header')) customElements.define('sticky-header', StickyHeader);
{% endjavascript %}

{% schema %}
{
  "name": "t:names.header",
  "class": "section-header-main",
  "blocks": [
    { "type": "@app" },
    {
      "type": "menu_tab",
      "name": "t:settings.menu_tab",
      "settings": [
        {
          "type": "header",
          "content": "t:content.logo"
        },
        {
          "type": "image_picker",
          "id": "logo",
          "label": "t:settings.logo"
        },
        {
          "type": "liquid",
          "id": "logo_text",
          "label": "t:settings.title",
          "default": "Ella"
        },
        {
          "type": "header",
          "content": "t:content.menu"
        },
        {
          "type": "page",
          "id": "link",
          "label": "t:settings.link"
        },
        {
          "type": "header",
          "content": "t:content.appearance"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "t:settings.color",
          "default": "#FFFFFF"
        },
        {
          "type": "color",
          "id": "text_bg",
          "label": "t:settings.background_color",
          "default": "#000000"
        },
        {
          "type": "color",
          "id": "text_color_active",
          "label": "t:settings.color_active",
          "default": "#000000"
        },
        {
          "type": "color",
          "id": "text_bg_active",
          "label": "t:settings.background_color_active",
          "default": "#FFFFFF"
        },
        {
          "type": "checkbox",
          "id": "divider",
          "label": "t:settings.divider",
          "default": false
        }
      ]
    },
    {
      "type": "mega_menu_1",
      "name": "t:names.mega_menu_style_1",
      "settings": [
        {
          "type": "select",
          "id": "grid",
          "options": [
            { "value": "2", "label": "2" },
            { "value": "3", "label": "3" },
            { "value": "4", "label": "4" },
            { "value": "5", "label": "5" },
            { "value": "6", "label": "6" },
            { "value": "7", "label": "7" },
            { "value": "8", "label": "8" }
          ],
          "default": "4",
          "label": "t:settings.grid_for_menus"
        },
        {
          "type": "text",
          "id": "menu_item",
          "label": "t:settings.menu_item_name",
          "info": "t:info.menu_item_name_info"
        },
        {
          "type": "header",
          "content": "t:content.size"
        },
        {
          "type": "select",
          "id": "width",
          "label": "t:settings.width",
          "options": [
            {
              "value": "page-width",
              "label": "t:options.page"
            },
            {
              "value": "full",
              "label": "t:options.full"
            }
          ],
          "default": "full"
        },
        {
          "type": "header",
          "content": "t:content.promotion_banner"
        },
        {
          "type": "checkbox",
          "id": "show_promotion_banner",
          "default": true,
          "label": "t:settings.show_promotion_banner"
        },
        {
          "type": "select",
          "id": "promotion_banner_layout",
          "options": [
            {
              "value": "left",
              "label": "t:options.left_large_block"
            },
            {
              "value": "right",
              "label": "t:options.right_large_block"
            }
          ],
          "default": "left",
          "label": "t:settings.collage_layout"
        },
        {
          "type": "range",
          "id": "image_border_thickness",
          "min": 0,
          "max": 24,
          "step": 1,
          "unit": "px",
          "label": "t:settings.image_border_thickness",
          "default": 1
        },
        {
          "type": "range",
          "id": "image_corner_radius",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "label": "t:settings.image_corner_radius",
          "default": 0
        },
        {
          "type": "header",
          "content": "t:content.first_promotion_banner"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "t:settings.image"
        },
        {
          "type": "url",
          "id": "link_url_1",
          "label": "t:settings.link"
        },
        {
          "type": "header",
          "content": "t:content.second_promotion_banner"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "t:settings.image"
        },
        {
          "type": "url",
          "id": "link_url_2",
          "label": "t:settings.link"
        },
        {
          "type": "header",
          "content": "t:content.third_promotion_banner"
        },
        {
          "type": "image_picker",
          "id": "image_3",
          "label": "t:settings.image"
        },
        {
          "type": "url",
          "id": "link_url_3",
          "label": "t:settings.link"
        }
      ]
    },
    {
      "type": "mega_menu_2",
      "name": "t:names.mega_menu_style_2",
      "settings": [
        {
          "type": "text",
          "id": "menu_item",
          "label": "t:settings.menu_item_name",
          "info": "t:info.menu_item_name_info"
        },
        {
          "type": "select",
          "id": "grid",
          "options": [
            { "value": "2", "label": "2" },
            { "value": "3", "label": "3" },
            { "value": "4", "label": "4" },
            { "value": "5", "label": "5" },
            { "value": "6", "label": "6" },
            { "value": "7", "label": "7" },
            { "value": "8", "label": "8" }
          ],
          "default": "4",
          "label": "t:settings.grid_for_menus"
        },
        {
          "type": "header",
          "content": "t:content.size"
        },
        {
          "type": "select",
          "id": "width",
          "label": "t:settings.width",
          "options": [
            {
              "value": "page-width",
              "label": "t:options.page"
            },
            {
              "value": "full",
              "label": "t:options.full"
            }
          ],
          "default": "full"
        },
        {
          "type": "header",
          "content": "t:content.promotion_banner"
        },
        {
          "type": "checkbox",
          "id": "show_promotion_banner",
          "default": true,
          "label": "t:settings.show_promotion_banner"
        },
        {
          "type": "select",
          "id": "promotion_banner_layout",
          "options": [
            {
              "value": "left",
              "label": "t:options.left_large_block"
            },
            {
              "value": "right",
              "label": "t:options.right_large_block"
            }
          ],
          "default": "left",
          "label": "t:settings.collage_layout"
        },
        {
          "type": "range",
          "id": "image_border_thickness",
          "min": 0,
          "max": 24,
          "step": 1,
          "unit": "px",
          "label": "t:settings.image_border_thickness",
          "default": 1
        },
        {
          "type": "range",
          "id": "image_corner_radius",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "label": "t:settings.image_corner_radius",
          "default": 0
        },
        {
          "type": "checkbox",
          "id": "inherit_color_scheme",
          "label": "t:settings.inherit_color_scheme",
          "default": true
        },
        {
          "type": "color_scheme",
          "id": "color_scheme",
          "label": "t:settings.color_scheme",
          "default": "scheme-1",
          "visible_if": "{{ block.settings.inherit_color_scheme == false }}"
        },
        {
          "type": "header",
          "content": "t:content.first_promotion_banner"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "t:settings.image"
        },
        {
          "type": "url",
          "id": "link_url_1",
          "label": "t:settings.link"
        },
        {
          "type": "header",
          "content": "t:content.second_promotion_banner"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "t:settings.image"
        },
        {
          "type": "url",
          "id": "link_url_2",
          "label": "t:settings.link"
        },
        {
          "type": "header",
          "content": "t:content.a_to_z_brands"
        },
        {
          "type": "checkbox",
          "id": "show_brand",
          "label": "t:settings.show_a_to_z_brands",
          "default": false
        },
        {
          "type": "text",
          "id": "brand_title",
          "label": "t:settings.title",
          "default": "All Brands A-Z",
          "visible_if": "{{ block.settings.show_brand == true }}"
        },
        {
          "type": "text",
          "id": "brand_text",
          "label": "t:settings.button_label",
          "default": "See All Brands",
          "visible_if": "{{ block.settings.show_brand == true }}"
        },
        {
          "type": "url",
          "id": "brand_url",
          "label": "t:settings.button_link",
          "visible_if": "{{ block.settings.show_brand == true }}"
        }
      ]
    },
    {
      "type": "mega_menu_3",
      "name": "t:names.mega_menu_style_3",
      "settings": [
        {
          "type": "text",
          "id": "menu_item",
          "label": "t:settings.menu_item_name",
          "info": "t:info.menu_item_name_info"
        },
        {
          "type": "select",
          "id": "grid",
          "options": [
            { "value": "2", "label": "2" },
            { "value": "3", "label": "3" },
            { "value": "4", "label": "4" },
            { "value": "5", "label": "5" },
            { "value": "6", "label": "6" },
            { "value": "7", "label": "7" },
            { "value": "8", "label": "8" }
          ],
          "default": "4",
          "label": "t:settings.grid_for_menus"
        },
        {
          "type": "header",
          "content": "t:content.size"
        },
        {
          "type": "select",
          "id": "width",
          "label": "t:settings.width",
          "options": [
            {
              "value": "page-width",
              "label": "t:options.page"
            },
            {
              "value": "full",
              "label": "t:options.full"
            }
          ],
          "default": "full"
        },
        {
          "type": "header",
          "content": "t:content.submenu"
        },
        {
          "type": "link_list",
          "id": "megamenu_submenu",
          "label": "t:settings.custom_submenu"
        },
        {
          "type": "header",
          "content": "t:content.product"
        },
        {
          "type": "collection",
          "id": "product_collection",
          "label": "t:settings.collection"
        },
        {
          "type": "range",
          "id": "product_limit",
          "label": "t:settings.limit",
          "min": 1,
          "max": 4,
          "step": 1,
          "default": 4
        },
        {
          "type": "select",
          "id": "product_direction",
          "label": "t:settings.direction",
          "options": [
            {
              "value": "column",
              "label": "t:options.vertical"
            },
            {
              "value": "row",
              "label": "t:options.horizontal"
            }
          ],
          "default": "column"
        },
        {
          "type": "select",
          "id": "horizontal_alignment",
          "label": "t:settings.alignment",
          "options": [
            {
              "value": "items-start",
              "label": "t:options.left"
            },
            {
              "value": "items-center",
              "label": "t:options.center"
            },
            {
              "value": "items-end",
              "label": "t:options.right"
            }
          ],
          "default": "items-start",
          "visible_if": "{{ block.settings.product_direction == 'row' }}"
        },
        {
          "type": "select",
          "id": "vertical_alignment_flex_direction_column",
          "label": "t:settings.alignment",
          "options": [
            {
              "value": "justify-start",
              "label": "t:options.left"
            },
            {
              "value": "justify-center",
              "label": "t:options.center"
            },
            {
              "value": "justify-end",
              "label": "t:options.right"
            }
          ],
          "default": "justify-start",
          "visible_if": "{{ block.settings.product_direction != 'row' }}"
        },
        {
          "type": "header",
          "content": "t:names.media_banner"
        },
        {
          "type": "checkbox",
          "id": "show_promotion_banner",
          "default": true,
          "label": "t:settings.show_promotion_banner"
        },
        {
          "type": "select",
          "id": "banner_row",
          "label": "t:settings.row",
          "options": [
            {
              "value": "top",
              "label": "t:options.top"
            },
            {
              "value": "bottom",
              "label": "t:options.bottom"
            }
          ],
          "default": "top",
          "visible_if": "{{ block.settings.show_promotion_banner == true }}"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "t:settings.image",
          "visible_if": "{{ block.settings.show_promotion_banner == true }}"
        },
        {
          "type": "range",
          "id": "image_border_thickness",
          "min": 0,
          "max": 24,
          "step": 1,
          "unit": "px",
          "label": "t:settings.image_border_thickness",
          "default": 1,
          "visible_if": "{{ block.settings.show_promotion_banner == true }}"
        },
        {
          "type": "range",
          "id": "image_corner_radius",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "label": "t:settings.image_corner_radius",
          "default": 0,
          "visible_if": "{{ block.settings.show_promotion_banner == true }}"
        },
        {
          "type": "url",
          "id": "link_url_1",
          "label": "t:settings.link",
          "visible_if": "{{ block.settings.show_promotion_banner == true }}"
        },
        {
          "type": "header",
          "content": "t:content.a_to_z_brands"
        },
        {
          "type": "checkbox",
          "id": "show_brand",
          "label": "t:settings.show_a_to_z_brands",
          "default": false
        },
        {
          "type": "text",
          "id": "brand_title",
          "label": "t:settings.title",
          "default": "All Brands A-Z"
        },
        {
          "type": "text",
          "id": "brand_text",
          "label": "t:settings.button_label",
          "default": "See All Brands"
        },
        {
          "type": "url",
          "id": "brand_url",
          "label": "t:settings.button_link"
        },
        {
          "type": "header",
          "content": "t:content.bottom_block"
        },
        {
          "type": "checkbox",
          "id": "inherit_color_scheme",
          "label": "t:settings.inherit_color_scheme",
          "default": true
        },
        {
          "type": "color_scheme",
          "id": "color_scheme",
          "label": "t:settings.color_scheme",
          "default": "scheme-1",
          "visible_if": "{{ block.settings.inherit_color_scheme == false }}"
        }
      ]
    },
    {
      "type": "vertical_menu",
      "name": "t:names.vertical_menu",
      "limit": 1,
      "settings": [
        {
          "type": "link_list",
          "id": "vertical_menu",
          "default": "main-menu",
          "label": "t:settings.menu"
        },
        {
          "type": "select",
          "id": "vertical_menu_style",
          "options": [
            { "value": "none", "label": "t:options.none" },
            { "value": "style_1", "label": "t:options.style_1" }
          ],
          "default": "style_1",
          "label": "t:settings.vertical_menu_style"
        },
        {
          "type": "text",
          "label": "t:settings.title",
          "id": "vertical_menu_title",
          "default": "Dropdown List"
        },
        {
          "type": "header",
          "content": "Button style"
        },
        {
          "type": "checkbox",
          "id": "inherit_color_scheme",
          "label": "t:settings.inherit_color_scheme",
          "default": true
        },
        {
          "type": "color",
          "id": "scheme_background",
          "label": "t:settings.background",
          "default": "#000000",
          "alpha": true,
          "visible_if": "{{ block.settings.inherit_color_scheme == false }}"
        },
        {
          "type": "color_background",
          "id": "scheme_background_gradient",
          "label": "t:content.background_gradient",
          "info": "t:info.background_gradient_info",
          "visible_if": "{{ block.settings.inherit_color_scheme == false }}"
        },
        {
          "type": "color",
          "id": "scheme_foreground",
          "label": "t:settings.text",
          "default": "#FFFFFF",
          "alpha": true,
          "visible_if": "{{ block.settings.inherit_color_scheme == false }}"
        },
        {
          "type": "checkbox",
          "id": "button_divider",
          "label": "t:settings.divider",
          "default": true
        },
        {
          "type": "select",
          "id": "button_width",
          "label": "t:settings.width",
          "options": [
            {
              "value": "fit",
              "label": "t:options.fit_content"
            },
            {
              "value": "large",
              "label": "t:options.large"
            }
          ],
          "default": "fit"
        },
        {
          "type": "range",
          "id": "button_border_radius",
          "min": 0,
          "max": 40,
          "step": 1,
          "unit": "px",
          "label": "t:settings.border_radius",
          "default": 0
        },
        {
          "type": "header",
          "content": "t:settings.appearance"
        },
        {
          "type": "color_scheme",
          "id": "color_scheme_vertical_menu",
          "label": "t:settings.color_scheme",
          "default": "scheme-1"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "t:content.logo"
    },
    {
      "type": "range",
      "id": "logo_width",
      "min": 50,
      "max": 300,
      "step": 5,
      "default": 100,
      "unit": "px",
      "label": "t:settings.logo_width",
      "visible_if": "{{ settings.logo != blank }}"
    },
    {
      "type": "range",
      "id": "logo_width_mobile",
      "min": 50,
      "max": 300,
      "step": 10,
      "default": 100,
      "unit": "px",
      "label": "t:settings.width_mobile",
      "visible_if": "{{ settings.logo != blank }}"
    },
    {
      "type": "select",
      "id": "logo_position",
      "label": "t:settings.position",
      "options": [
        {
          "value": "left",
          "label": "t:options.left"
        },
        {
          "value": "center",
          "label": "t:options.center"
        },
        {
          "value": "right",
          "label": "t:options.right"
        }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "logo_row",
      "label": "t:settings.row",
      "options": [
        {
          "value": "top",
          "label": "t:options.top"
        },
        {
          "value": "bottom",
          "label": "t:options.bottom"
        }
      ],
      "default": "top"
    },
    {
      "type": "header",
      "content": "t:content.menu"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:settings.menu",
      "default": "main-menu"
    },
    {
      "type": "select",
      "id": "menu_position",
      "label": "t:settings.position",
      "options": [
        {
          "value": "left",
          "label": "t:options.left"
        },
        {
          "value": "center",
          "label": "t:options.center"
        },
        {
          "value": "right",
          "label": "t:options.right"
        }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "menu_row",
      "label": "t:settings.row",
      "options": [
        {
          "value": "top",
          "label": "t:options.top"
        },
        {
          "value": "bottom",
          "label": "t:options.bottom"
        }
      ],
      "default": "top"
    },
    {
      "type": "select",
      "id": "icon_type",
      "label": "t:settings.icon",
      "options": [
        {
          "value": "none",
          "label": "t:options.none"
        },
        {
          "value": "caret",
          "label": "t:options.caret"
        }
      ],
      "default": "none"
    },
    {
      "type": "header",
      "content": "t:content.message"
    },
    {
      "type": "checkbox",
      "id": "show_message",
      "label": "t:settings.show_message",
      "default": true
    },
    {
      "type": "richtext",
      "id": "message",
      "label": "t:settings.message",
      "default": "<p>Summer Sale – Up to 30% OFF</p>",
      "visible_if": "{{ section.settings.show_message }}"
    },
    {
      "type": "select",
      "id": "font",
      "label": "t:settings.font",
      "options": [
        {
          "value": "var(--font-body--family)",
          "label": "t:options.body"
        },
        {
          "value": "var(--font-subheading--family)",
          "label": "t:options.subheading"
        },
        {
          "value": "var(--font-heading--family)",
          "label": "t:options.heading"
        }
      ],
      "default": "var(--font-body--family)",
      "visible_if": "{{ section.settings.show_message }}"
    },
    {
      "type": "select",
      "id": "font_size",
      "label": "t:settings.size",
      "options": [
        {
          "value": "1rem",
          "label": "10px"
        },
        {
          "value": "1.2rem",
          "label": "12px"
        },
        {
          "value": "1.4rem",
          "label": "14px"
        },
        {
          "value": "1.6rem",
          "label": "16px"
        },
        {
          "value": "1.8rem",
          "label": "18px"
        }
      ],
      "default": "1rem",
      "visible_if": "{{ section.settings.show_message }}"
    },
    {
      "type": "select",
      "id": "message_position",
      "label": "t:settings.position",
      "options": [
        {
          "value": "left",
          "label": "t:options.left"
        },
        {
          "value": "right",
          "label": "t:options.right"
        }
      ],
      "default": "right"
    },
    {
      "type": "select",
      "id": "message_row",
      "label": "t:settings.row",
      "options": [
        {
          "value": "top",
          "label": "t:options.top"
        },
        {
          "value": "bottom",
          "label": "t:options.bottom"
        }
      ],
      "default": "top",
      "visible_if": "{{ section.settings.show_message }}"
    },
    {
      "type": "header",
      "content": "t:content.customer_account"
    },
    {
      "type": "paragraph",
      "content": "t:content.manage_customer_accounts"
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "label": "t:settings.account",
      "default": true
    },
    {
      "type": "select",
      "id": "account_type",
      "label": "t:settings.account_type",
      "options": [
        {
          "value": "text",
          "label": "t:options.text"
        },
        {
          "value": "icon",
          "label": "t:options.icon"
        },
        {
          "value": "both",
          "label": "Both"
        }
      ],
      "default": "icon"
    },
    {
      "type": "select",
      "id": "account_row",
      "label": "t:settings.row",
      "options": [
        {
          "value": "top",
          "label": "t:options.top"
        },
        {
          "value": "bottom",
          "label": "t:options.bottom"
        }
      ],
      "default": "top",
      "visible_if": "{{ section.settings.show_account }}"
    },
    {
      "type": "header",
      "content": "t:content.search"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "t:settings.search_icon",
      "default": true
    },
    {
      "type": "select",
      "id": "search_type",
      "label": "t:settings.type",
      "visible_if": "{{ section.settings.show_search and settings.search_type == 'inline' }}",
      "options": [
        {
          "value": "frame",
          "label": "Frame"
        },
        {
          "value": "underline",
          "label": "Underline"
        }
      ],
      "default": "frame"
    },
    {
      "type": "select",
      "id": "search_position",
      "label": "t:settings.search_position",
      "options": [
        {
          "value": "left",
          "label": "t:options.left"
        },
        {
          "value": "center",
          "label": "t:options.center"
        },
        {
          "value": "right",
          "label": "t:options.right"
        }
      ],
      "default": "right",
      "visible_if": "{{ section.settings.show_search }}"
    },
    {
      "type": "select",
      "id": "search_row",
      "label": "t:settings.search_row",
      "options": [
        {
          "value": "top",
          "label": "t:options.top"
        },
        {
          "value": "bottom",
          "label": "t:options.bottom"
        }
      ],
      "default": "top",
      "visible_if": "{{ section.settings.show_search }}"
    },
    {
      "type": "header",
      "content": "t:content.localization"
    },
    {
      "type": "checkbox",
      "id": "show_country",
      "label": "t:settings.country_region",
      "info": "t:info.manage_countries_regions",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_language",
      "label": "t:settings.language_selector",
      "info": "t:info.manage_languages",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "country_selector_style",
      "label": "t:settings.flag",
      "default": true,
      "visible_if": "{{ section.settings.show_country or section.settings.show_language }}"
    },
    {
      "type": "select",
      "id": "localization_font",
      "label": "t:settings.font",
      "options": [
        {
          "value": "heading",
          "label": "t:options.heading"
        },
        {
          "value": "subheading",
          "label": "t:options.subheading"
        },
        {
          "value": "body",
          "label": "t:options.body"
        },
        {
          "value": "accent",
          "label": "t:options.accent"
        }
      ],
      "default": "heading",
      "visible_if": "{{ section.settings.show_country or section.settings.show_language }}"
    },
    {
      "type": "select",
      "id": "localization_font_size",
      "label": "t:settings.size",
      "options": [
        {
          "value": "1rem",
          "label": "10px"
        },
        {
          "value": "1.2rem",
          "label": "12px"
        },
        {
          "value": "1.4rem",
          "label": "14px"
        },
        {
          "value": "1.6rem",
          "label": "16px"
        },
        {
          "value": "1.8rem",
          "label": "18px"
        }
      ],
      "default": "1rem"
    },
    {
      "type": "select",
      "id": "localization_type",
      "label": "t:settings.localization_type",
      "options": [
        {
          "value": "text",
          "label": "t:options.text"
        },
        {
          "value": "icon",
          "label": "t:options.icon"
        }
      ],
      "default": "icon"
    },
    {
      "type": "select",
      "id": "localization_position",
      "label": "t:settings.position",
      "options": [
        {
          "value": "left",
          "label": "t:options.left"
        },
        {
          "value": "right",
          "label": "t:options.right"
        }
      ],
      "default": "right",
      "visible_if": "{{ section.settings.show_country or section.settings.show_language }}"
    },
    {
      "type": "select",
      "id": "localization_row",
      "label": "t:settings.row",
      "options": [
        {
          "value": "top",
          "label": "t:options.top"
        },
        {
          "value": "bottom",
          "label": "t:options.bottom"
        }
      ],
      "default": "top",
      "visible_if": "{{ section.settings.show_country or section.settings.show_language }}"
    },
    {
      "type": "header",
      "content": "t:content.actions"
    },
    {
      "type": "checkbox",
      "id": "gift_cards_enabled",
      "label": "Gift cards icon",
      "default": false
    },
    {
      "type": "url",
      "id": "link_giftcards",
      "label": "Gift cards link",
      "visible_if": "{{ section.settings.gift_cards_enabled }}"
    },
    {
      "type": "select",
      "id": "actions_type",
      "label": "t:settings.type",
      "options": [
        {
          "value": "text",
          "label": "t:options.text"
        },
        {
          "value": "icon",
          "label": "t:options.icon"
        },
        {
          "value": "both",
          "label": "Both"
        }
      ],
      "default": "icon"
    },
    {
      "type": "select",
      "id": "actions_row",
      "label": "t:settings.row",
      "options": [
        {
          "value": "top",
          "label": "t:options.top"
        },
        {
          "value": "bottom",
          "label": "t:options.bottom"
        }
      ],
      "default": "top"
    },
    {
      "type": "header",
      "content": "t:content.appearance"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "select",
      "id": "enable_sticky_header",
      "label": "t:settings.sticky_header",
      "options": [
        {
          "value": "always",
          "label": "t:options.always"
        },
        {
          "value": "scroll-up",
          "label": "t:options.on_scroll_up"
        },
        {
          "value": "never",
          "label": "t:options.never"
        }
      ],
      "default": "always"
    },
    {
      "type": "range",
      "id": "divider_width",
      "label": "t:settings.divider_thickness",
      "min": 0,
      "max": 5,
      "step": 0.5,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.menu_row == 'bottom' or section.settings.localization_row == 'bottom' or section.settings.search_row == 'bottom' or section.settings.account_row == 'bottom' or section.settings.actions_row == 'bottom' }}"
    },
    {
      "type": "select",
      "id": "divider_size",
      "label": "t:settings.divider_width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "visible_if": "{{ section.settings.menu_row == 'bottom' or section.settings.localization_row == 'bottom' or section.settings.search_row == 'bottom' or section.settings.account_row == 'bottom' or section.settings.actions_row == 'bottom' }}"
    },
    {
      "type": "range",
      "id": "border_width",
      "label": "t:settings.border_width",
      "min": 0,
      "max": 5,
      "step": 0.5,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:content.colors"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme_top",
      "label": "t:settings.default",
      "default": "primary"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme_search_inline",
      "label": "t:settings.search",
      "default": "primary",
      "visible_if": "{{ settings.search_type == 'inline' }}"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme_bottom",
      "label": "t:settings.bottom_row",
      "default": "primary",
      "visible_if": "{{ section.settings.menu_row == 'bottom' or section.settings.localization_row == 'bottom' or section.settings.search_row == 'bottom' or section.settings.account_row == 'bottom' or section.settings.actions_row == 'bottom' }}"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme_transparent",
      "label": "t:settings.inverse",
      "default": "primary",
      "visible_if": "{{ section.settings.enable_transparent_header_home or section.settings.enable_transparent_header_product or section.settings.enable_transparent_header_collection }}"
    },
    {
      "type": "color_scheme",
      "id": "menu_mobile_color_scheme",
      "label": "t:settings.mobile_menu",
      "default": "primary"
    },
    {
      "type": "header",
      "content": "Highlighted items"
    },
    {
      "type": "textarea",
      "id": "highlighted_item_1",
      "label": "t:settings.menu_level_1_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "textarea",
      "id": "highlighted_item_2",
      "label": "t:settings.menu_level_2_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "textarea",
      "id": "highlighted_item_3",
      "label": "t:settings.menu_level_3_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "color",
      "id": "highlighted_item_color",
      "label": "t:settings.color",
      "default": "#D12442"
    },
    {
      "type": "header",
      "content": "Label on menu"
    },
    {
      "type": "paragraph",
      "content": "New label"
    },
    {
      "type": "textarea",
      "id": "new_label_1",
      "label": "t:settings.menu_level_1_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "textarea",
      "id": "new_label_2",
      "label": "t:settings.menu_level_2_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "textarea",
      "id": "new_label_3",
      "label": "t:settings.menu_level_3_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "color",
      "id": "new_label_color",
      "label": "t:settings.color",
      "default": "#FFFFFF"
    },
    {
      "type": "color_background",
      "id": "new_label_bg",
      "label": "t:settings.background",
      "default": "#BAE4ED"
    },
    {
      "type": "paragraph",
      "content": "Sale label"
    },
    {
      "type": "textarea",
      "id": "sale_label_1",
      "label": "t:settings.menu_level_1_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "textarea",
      "id": "sale_label_2",
      "label": "t:settings.menu_level_2_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "textarea",
      "id": "sale_label_3",
      "label": "t:settings.menu_level_3_items",
      "info": "t:info.highlighted_item_info"
    },
    {
      "type": "color",
      "id": "sale_label_color",
      "label": "t:settings.color",
      "default": "#FFFFFF"
    },
    {
      "type": "color_background",
      "id": "sale_label_bg",
      "label": "t:settings.background",
      "default": "#ff1a00"
    },
    {
      "type": "header",
      "content": "t:content.home_page"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header_home",
      "label": "t:settings.transparent_background",
      "default": false
    },
    {
      "type": "select",
      "id": "home_color_scheme",
      "label": "t:settings.color_scheme",
      "options": [
        {
          "value": "default",
          "label": "t:options.default"
        },
        {
          "value": "inverse",
          "label": "t:options.inverse"
        }
      ],
      "default": "default",
      "visible_if": "{{ section.settings.enable_transparent_header_home }}"
    },
    {
      "type": "header",
      "content": "t:content.product_page"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header_product",
      "label": "t:settings.transparent_background",
      "default": false
    },
    {
      "type": "select",
      "id": "product_color_scheme",
      "label": "t:settings.color_scheme",
      "options": [
        {
          "value": "default",
          "label": "t:options.default"
        },
        {
          "value": "inverse",
          "label": "t:options.inverse"
        }
      ],
      "default": "default",
      "visible_if": "{{ section.settings.enable_transparent_header_product }}"
    },
    {
      "type": "header",
      "content": "t:content.collection_page"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header_collection",
      "label": "t:settings.transparent_background",
      "default": false
    },
    {
      "type": "select",
      "id": "collection_color_scheme",
      "label": "t:settings.color_scheme",
      "options": [
        {
          "value": "default",
          "label": "t:options.default"
        },
        {
          "value": "inverse",
          "label": "t:options.inverse"
        }
      ],
      "default": "default",
      "visible_if": "{{ section.settings.enable_transparent_header_collection }}"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ]
}
{% endschema %}
